[tool:pytest]
testpaths = tests
asyncio_mode = auto
timeout = 30
addopts =
    --strict-markers
    --strict-config
    --verbose
    --tb=short
    -ra

markers =
    unit: Unit tests
    integration: Integration tests
    stress: Stress/load tests
    slow: Tests that take longer than 5 seconds